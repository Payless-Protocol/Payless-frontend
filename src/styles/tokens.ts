export const colors = {
  accent: "var(--accent)",
  bg: "var(--bg)",
  surface: "var(--surface)",
  success: "var(--success)",
  danger: "var(--danger)",
  textPrimary: "var(--text-primary)",
  textSecondary: "var(--text-secondary)",
} as const;

export const radii = {
  sm: "8px",
  md: "12px",
  lg: "20px",
  xl: "28px",
} as const;

export const layout = {
  contentWidth: "1280px",
} as const;
